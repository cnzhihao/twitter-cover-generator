# Twitter Cover Generator

一个用于生成 X / Twitter 封面图的静态 HTML 工具。

## Local Preview

```bash
python3 -m http.server 4173
```

Open `http://localhost:4173`.

## Deploy

This project is a static site. Vercel can deploy it directly from the repository root.

